import './spinner.scss';
import { FC } from 'react';

const Spinner: FC = () => (
  <div className="spinner">
    <div className="spinner__inner">
      <div />
    </div>
  </div>
);

export default Spinner;
